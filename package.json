{
  "name": "rm-sync-renderer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/render.ts --bundle --platform=node --target=node22 --outfile=render.js"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  }
}
